import { StealthDirective } from './stealth.directive';

describe('StealthDirective', () => {
  it('should create an instance', () => {
    const directive = new StealthDirective();
    expect(directive).toBeTruthy();
  });
});
