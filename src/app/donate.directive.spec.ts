import { DonateDirective } from './donate.directive';

describe('DonateDirective', () => {
  it('should create an instance', () => {
    const directive = new DonateDirective();
    expect(directive).toBeTruthy();
  });
});
